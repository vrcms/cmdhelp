# CmdHelp

跨平台命令行智能助手：只按命令名查询本机 man/Get-Help 的本地帮助，交大模型生成通俗中文解释，全程绝不执行用户输入的目标命令。

## Language

### 核心流程

**目标命令 (Target Command)**:
用户想要了解的命令，由命令名标识。工具只解释它，绝不执行它。
_Avoid_: 用户命令、要执行的命令

**命令名 (Command Name)**:
用户输入的第一个 token，经校验后作为唯一的帮助查询对象。
_Avoid_: 命令、命令名参数

**AI 解释 (Explanation)**:
大模型基于本地帮助生成的通俗说明，固定为功能简述、常用参数、典型示例三段。
_Avoid_: 回答、答案、解读

**来源标注 (Source Attribution)**:
解释中明确区分"来自本地帮助"与"通用知识"的做法，让用户能判断可信度。
_Avoid_: 引用、标注

**回退 (Fallback)**:
本地帮助不可用时仅凭模型通用知识解释，并以提示语明示"可能不准确"的流程。
_Avoid_: 兜底、降级模式

### 帮助获取

**本地帮助 (Local Help)**:
从本机 man 或 Get-Help 取回的命令文档原文，是 AI 解释的主要依据与准确性来源。
_Avoid_: 帮助文档、man page、help 输出

**帮助来源 (Help Source)**:
按平台分派的帮助获取器（man 或 Get-Help），对上层提供统一的取用接口。
_Avoid_: 帮助后端、帮助提供者

### 安全

**安全边界 (Safety Envelope)**:
工具只允许执行文档查询命令（man、Get-Help），绝不以任何方式运行目标命令或其参数；此边界不可配置、不可放松。
_Avoid_: 安全策略、防护措施

**坏输入 (Malformed Input)**:
无法通过命令名白名单校验的用户输入（以 `-` 开头、含路径分隔符、空 token 等）；直接拒绝查询并解释原因。
_Avoid_: 非法输入、攻击输入